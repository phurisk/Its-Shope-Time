import type { Metadata } from "next";
import { Poppins } from "next/font/google";
import "./globals.css";

const poppins = Poppins({
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700"],
  variable: "--font-poppins",
});

export const metadata: Metadata = {
  title: "It's Shopee Time - Discover What's Trending",
  description: "Your trusted source for the best Shopee deals, honest reviews, and trending products. Expertly curated daily updates.",
  keywords: "Shopee, deals, reviews, trending products, e-commerce, affiliate",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={`${poppins.variable} font-poppins antialiased`}>
        {children}
      </body>
    </html>
  );
}